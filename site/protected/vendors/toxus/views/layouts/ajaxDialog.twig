{# 
  a dialog to replace the main dialog which can be called in an ajax call for the dialog function
#}

{% block fullContent %}
	<div class="modal-header">
    <button type="button" class="close action-close" data-dismiss="modal" aria-hidden="true">×</button>
    {% block dialogHeader %}<h3>{{ title | default(Yii_t('base','Dialog')) }}</h3>{%endblock %}
  </div>
	<div class="row col-sm-offset-1" style="margin-top: 20px;">
	{% block content %}
		{{ content |raw }}
	{% endblock %}
	</div>
  <div class="modal-footer">
		{% block dialogFooter %}
    <button id="btn-close" class="btn caction-close" data-dismiss="modal">{{ Yii_t('button','btn-close') }}</button>
		{% endblock %}
  </div>
{% endblock %}
